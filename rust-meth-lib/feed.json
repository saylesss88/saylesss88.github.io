{
  "version": "https://jsonfeed.org/version/1.1",
  "title": "rust-meth-lib",
  "home_page_url": "https://example.com/",
  "feed_url": "https://example.com/feed.json",
  "description": "Description",
  "items": [
    {
      "id": "https://example.com/getting_started.html",
      "url": "https://example.com/getting_started.html",
      "title": "Getting Started",
      "content_html": "<h1>Getting Started</h1>\n<p>Runnable examples covering the main ways to embed <code>rust-meth-lib</code> in your own\ntooling. Each file is self-contained and documents its own <code>[dependencies]</code>\nsnippet.</p>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/basic_query.rs\"><code>basic_query.rs</code></a></h3>\n<p>This example first locates the <code>rust-analyzer</code> binary, then it queries the\navailable methods on a <code>RefCell&lt;u8&gt;</code> and prints the results.</p>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_methods};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    let methods = query_methods(\"RefCell&lt;u8&gt;\", &amp;ra_path, None)?;\n\n    println!(\"Methods on RefCell&lt;u8&gt; ({} total):\", methods.len());\n    for method in &amp;methods {\n        println!(\"  {}\", method.name);\n    }\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<p>The minimal end-to-end path: <code>find_rust_analyzer</code> → <code>query_methods</code> → print\nmethod names.</p>\n<pre><code>Methods on RefCell&lt;u8&gt; (30 total):\n  as_ptr\n  borrow\n  borrow_mut\n  clamp\n  clone\n  clone_from\n  clone_into\n  cmp\n  eq\n  ge\n  get_mut\n  gt\n  into\n  into_inner\n  le\n  lt\n  max\n  min\n  ne\n  partial_cmp\n  replace\n  replace_with\n  swap\n  take\n  to_owned\n  try_borrow\n  try_borrow_mut\n  try_borrow_unguarded\n  try_into\n  undo_leak\n</code></pre>\n<h3>Limit Number of Methods Returned from Standard API</h3>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{Method, find_rust_analyzer, query_methods};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    // Step 1: locate the rust-analyzer binary.\n    //\n    // find_rust_analyzer checks `PATH` first, then falls back to the rustup\n    // component directory (~/.rustup/toolchains/&lt;active&gt;/bin/rust-analyzer).\n    // Returns `RustMethError::RustAnalyzerNotFound` if neither exists.\n    let ra_path = find_rust_analyzer()?;\n\n    // Step 2: query methods.\n    //\n    // query_methods spins up an ephemeral LSP session, synthesises a\n    // temporary Cargo project with `let _x: RefCell&lt;u8&gt; = todo!();`, triggers\n    // a completion request at the dot position, and tears everything down.\n    //\n    // The third argument is `deps: Option&lt;&amp;str&gt;`: `None` means stdlib only.\n    let methods = query_methods(\"RefCell&lt;u8&gt;\", &amp;ra_path, None)?;\n\n    let top5: Vec&lt;Method&gt; = methods.into_iter().take(5).collect();\n\n    // println!(\"Methods on RefCell&lt;u8&gt; ({} total):\", methods.len());\n    println!(\"First 5 methods on RefCell&lt;u8&gt;:\");\n    // for method in &amp;methods {\n    for method in &amp;top5 {\n        println!(\"  {}\", method.name);\n    }\n\n    Ok(())\n}\n</code></pre>\n<p><strong>Output</strong></p>\n<pre><code>First 5 methods on RefCell&lt;u8&gt;:\n  as_ptr\n  borrow\n  borrow_mut\n  clamp\n  clone\n</code></pre>\n<h3>Using the Builder API</h3>\n<p>The same query using [<code>MethodQuery</code>], equivalent to the above but chainable:</p>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::find_rust_analyzer;\nuse rust_meth_lib::query::MethodQuery;\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n    let methods = MethodQuery::new(\"RefCell&lt;u8&gt;\")\n        .run(&amp;ra_path)?;\n\n    println!(\"Methods on RefCell&lt;u8&gt; ({} total):\", methods.len());\n    for method in &amp;methods {\n        println!(\"  {}\", method.name);\n    }\n    Ok(())\n}\n</code></pre>\n<p>The builder becomes more useful when combining options, adding a filter or\nresolving source locations without separate function calls:</p>\n<pre><code class=\"language-rs\">let methods = MethodQuery::new(\"RefCell&lt;u8&gt;\")\n    .filter(\"borrow\")\n    .run(&amp;ra_path)?;\n</code></pre>\n<p>With the filter above, the output is (I changed the print statement to match):</p>\n<pre><code>Methods on RefCell&lt;u8&gt; with borrow in their name (5 total):\n  borrow\n  borrow_mut\n  try_borrow\n  try_borrow_mut\n  try_borrow_unguarded\n</code></pre>\n<h3>Limit the Number of Methods Returned</h3>\n<p>You can use the <code>.limit()</code> method to return <code>N</code> methods. For example, a <code>Vec&lt;u8&gt;</code>,\nreturns over 200 methods. To only return the first 10 sorted alphabetically:</p>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::find_rust_analyzer;\nuse rust_meth_lib::query::MethodQuery;\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n    let methods = MethodQuery::new(\"Vec&lt;u8&gt;\").limit(10).run(&amp;ra_path)?;\n\n    println!(\"First 10 methods on Vec&lt;u8&gt;:\");\n    for method in &amp;methods {\n        println!(\"  {}\", method.name);\n    }\n    Ok(())\n}\n</code></pre>\n<p><strong>Output</strong></p>\n<pre><code>First 10 methods on Vec&lt;u8&gt;:\n  align_to\n  align_to_mut\n  allocator\n  append\n  array_windows\n  as_array\n  as_ascii\n  as_ascii_unchecked\n  as_chunks\n  as_chunks_mut\n</code></pre>\n",
      "date_published": "2026-08-31T00:00:00+00:00",
      "author": {
        "email": "saylesss87@proton.me",
        "name": "saylesss88"
      },
      "tags": [
        "rust-analyzer",
        "rust-meth-lib"
      ],
      "language": "en-US"
    },
    {
      "id": "https://example.com/README.html",
      "url": "https://example.com/README.html",
      "title": "rust-meth-lib",
      "content_html": "<h1>rust-meth-lib</h1>\n<p><a href=\"https://crates.io/crates/rust-meth-lib\"><img src=\"https://img.shields.io/crates/v/rust-meth-lib.svg\" alt=\"Crates.io\" /></a>\n<a href=\"https://docs.rs/rust-meth-lib\"><img src=\"https://docs.rs/rust-meth-lib/badge.svg\" alt=\"Documentation\" /></a>\n<a href=\"https://opensource.org/licenses/Apache-2.0\"><img src=\"https://img.shields.io/badge/License-Apache_2.0-blue.svg\" alt=\"License\" /></a></p>\n<p>Core library powering <a href=\"https://crates.io/crates/rust-meth\"><code>rust-meth</code></a>.\nDiscovers methods available on any Rust type by driving a <code>rust-analyzer</code> LSP\nsession.</p>\n<h2>Overview</h2>\n<p><code>rust-meth-lib</code> provides the building blocks for programmatically querying\n<code>rust-analyzer</code>:</p>\n<ul>\n<li>Spinning up and managing an LSP session (<code>LspTransport</code>)</li>\n<li>Synthesizing a temporary Rust project and injecting the target type (<code>Probe</code>)</li>\n<li>Requesting completions and go-to-definition results from <code>rust-analyzer</code></li>\n<li>Fuzzy-filtering and ranking method results</li>\n<li>Exposing utilities for embedding <code>rust-meth</code> workflows in other tools</li>\n<li>Full working <a href=\"https://example.com/README.html#rust-meth-lib-examples\">Examples</a></li>\n</ul>\n<p>Please file an Issue if any problems arise.</p>\n<h2>Workspace layout</h2>\n<pre><code>rust-meth/\n├── Cargo.toml               — workspace manifest\n├── lib/                     — rust-meth-lib (this crate)\n│   ├── Cargo.toml\n│   ├── README.md\n│   ├── benches/\n│   │   └── benchmarks.rs\n│   └── src/\n│       ├── lib.rs\n│       ├── error.rs\n│       ├── lsp.rs\n│       ├── probe.rs\n│       └── analyzer/\n│           ├── mod.rs\n│           ├── discovery.rs\n│           ├── parse.rs\n│           └── session.rs\n└── cli/                     : standalone binary crate that calls into this library\n    ├── Cargo.toml\n    └── src/\n        └── main.rs\n</code></pre>\n<blockquote>\n<p>UI components, CLI argument parsing, interactive selection, and spinner\ndisplay are now maintained in <a href=\"https://example.com/../cli/\"><code>cli/</code></a>.</p>\n</blockquote>\n<h2>Public API</h2>\n<h3>Key Types</h3>\n<table><thead><tr><th>Item</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>LspTransport</code></td><td>Manages the stdio JSON-RPC channel to a <code>rust-analyzer</code> process</td></tr>\n<tr><td><code>Probe</code></td><td>Builds a temporary Cargo project that makes the target type available for LSP queries</td></tr>\n<tr><td><code>analyzer</code></td><td>High-level session orchestrator, combines <code>Probe</code> + <code>LspTransport</code> to return method completions</td></tr>\n</tbody></table>\n<h3>Basic Usage</h3>\n<pre><code class=\"language-toml\">[dependencies]\nrust-meth-lib = \"0.3.0\"\n</code></pre>\n<ul>\n<li><a href=\"https://docs.rs/rust-meth-lib/latest/rust_meth_lib/index.html\">rust_meth_lib docs.rs</a></li>\n</ul>\n<p><strong>Full working Example</strong>:</p>\n<pre><code class=\"language-rust\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_methods};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n    let methods = query_methods(\"Result&lt;u8, &amp;'static str&gt;\", &amp;ra_path, None)?;\n\n    println!(\"Methods available for Result&lt;u8, &amp;'static str&gt;\");\n    for method in &amp;methods {\n        println!(\"{}\", method.name);\n    }\n\n    Ok(())\n}\n</code></pre>\n<p>The exact list returned varies by toolchain.</p>\n<blockquote class=\"markdown-alert-note\">\n<p>The primary APIs are intended for embedding or extending the <code>rust-meth</code>\nworkflow.<br />\nThe CLI interface is maintained in a separate crate.</p>\n</blockquote>\n<h3>Builder API</h3>\n<p><a href=\"https://example.com/examples/builder_run.rs\"><code>MethodQuery</code></a> provides a chainable interface over\n<code>query_methods</code> and <code>filter_methods</code>. Use <code>.filter()</code> to narrow results by\nmatch quality (exact &gt; prefix &gt; substring), and <code>.run_with_definitions()</code> to\nalso resolve source locations.</p>\n<pre><code class=\"language-rust\">// Query + filter\nMethodQuery::new(\"String\")\n    .filter(\"push\")\n    .run(&amp;ra_path)?;\n\n// Query + filter + source locations\nMethodQuery::new(\"serde_json::Value\")\n    .deps(r#\"serde_json = \"1.0\"\"#)\n    .filter(\"as_str\")\n    .run_with_definitions(&amp;ra_path)?;\n</code></pre>\n<p>See <a href=\"https://example.com/examples/builder_run.rs\"><code>builder_run.rs</code></a> and\n<a href=\"https://example.com/examples/builder_definitions.rs\"><code>builder_definitions.rs</code></a> for full examples.</p>\n<h2>Error Handling</h2>\n<p>The library uses a single\n<a href=\"https://docs.rs/rust-meth-lib/latest/rust_meth_lib/error/enum.RustMethError.html\"><code>RustMethError</code></a>\nenum (via <a href=\"https://docs.rs/thiserror\"><code>thiserror</code></a>) and a <code>Result&lt;T&gt;</code> alias over\nit:</p>\n<pre><code class=\"language-rs\">pub type Result&lt;T&gt; = std::result::Result&lt;T, RustMethError&gt;;\n</code></pre>\n<table><thead><tr><th>Variant</th><th>Cause</th></tr></thead><tbody>\n<tr><td><code>Io</code></td><td>OS / file system errors (<code>std::io::Error</code>)</td></tr>\n<tr><td><code>Json</code></td><td>JSON serialization or deserialization failure</td></tr>\n<tr><td><code>ParseInt</code></td><td>String-to-integer conversion failure</td></tr>\n<tr><td><code>NoContentLength</code></td><td>LSP response missing a <code>Content-Length</code> header</td></tr>\n<tr><td><code>RecvExhausted { limit }</code></td><td>Message loop hit <code>limit</code> without matching a response</td></tr>\n<tr><td><code>UnexpectedResponseShape</code></td><td>Structurally valid JSON but missing expected fields</td></tr>\n<tr><td><code>RustAnalyzerNotFound</code></td><td><code>rust-analyzer</code> not on <code>PATH</code> or in component directory</td></tr>\n<tr><td><code>MissingStdin</code></td><td><code>stdin</code> handle wasn’t captured</td></tr>\n<tr><td><code>MissingStdout</code></td><td><code>stdout</code> handle wasn’t captured</td></tr>\n<tr><td><code>TypeNotFound</code></td><td><code>rust-analyzer</code> diagnostic error for the probed type</td></tr>\n<tr><td><code>Timeout</code></td><td><code>rust-analyzer</code> failed to produce a usable response within retry budget</td></tr>\n<tr><td><code>FeatureGated</code></td><td>Type exists but requires a feature flag that wasn’t enabled for this probe</td></tr>\n</tbody></table>\n<p>All fallible functions in this crate return <code>rust_meth_lib::Result&lt;T&gt;</code>. If\nyou’re embedding the library, you can convert into your own error type via <code>?</code>\nsince <code>RustMethError</code> implements <code>std::error::Error</code>.</p>\n<pre><code class=\"language-rust\">use rust_meth_lib::{Result, analyzer::{find_rust_analyzer, query_methods}};\n\nfn my_wrapper() -&gt; Result&lt;()&gt; {\n    // RustAnalyzerNotFound, Io, Json, etc. all propagate with ?\n    let ra_path = find_rust_analyzer()?;\n    let _methods = query_methods(\"Vec&lt;u8&gt;\", &amp;ra_path, None)?;\n    Ok(())\n}\n</code></pre>\n<h1>rust-meth-lib examples</h1>\n<p>Runnable examples covering the main ways to embed <code>rust-meth-lib</code> in your own\ntooling. Each file is self-contained and documents its own <code>[dependencies]</code>\nsnippet.</p>\n<h2>Running the Examples</h2>\n<p>To run the examples locally, clone the repo:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/saylesss88/rust-meth.git\ncd rust-meth/lib\ncargo run --example basic_query\n</code></pre>\n<p>The examples require <code>rust-analyzer</code> on your <code>PATH</code>:</p>\n<pre><code class=\"language-sh\">rustup component add rust-analyzer\n</code></pre>\n<pre><code class=\"language-sh\">cargo run --example basic_query\ncargo run --example method_info\ncargo run --example third_party_crate   # slow on first run (downloads serde_json)\ncargo run --example fuzzy_filter\ncargo run --example go_to_definition\ncargo run --example custom_error\ncargo run --example batch_query\n</code></pre>\n<p>Examples start at <a href=\"https://saylesss88.github.io/rust-meth-lib/getting_started.html\">Getting Started</a></p>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/querying-methods.html",
      "url": "https://example.com/querying-methods.html",
      "title": "Querying Methods",
      "content_html": "<h1>Querying Methods</h1>\n<p>Shows how to work with the richer fields on [<code>Method</code>] beyond just the name:</p>\n<ul>\n<li><code>Method::detail</code>: the full function signature as a string (e.g.\n<code>\"const fn(&amp;self) -&gt; *mut T\"</code>), returned by <code>rust-analyzer</code> as the completion\nitem’s detail field. <code>Option&lt;String&gt;</code>: not always present, especially for\ntrait-provided methods on generic types.</li>\n<li><code>Method::documentation</code>: the rustdoc string for the method, often multi-line.\n<code>Option&lt;String&gt;</code>: coverage varies by crate and method.</li>\n</ul>\n<p>Both fields are <code>None</code> when <code>rust-analyzer</code> doesn’t return them. Methods with\nneither still appear in the results from <code>query_methods</code>. This example filters\nto only those with at least one field populated, and renders them gracefully\nwhen the other is absent.</p>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/method_info.rs\">method_info.rs</a></h3>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{Method, find_rust_analyzer, query_methods};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    let methods = query_methods(\"Cell&lt;u8&gt;\", &amp;ra_path, None)?;\n\n    // Only print methods that have at least a signature. Methods with\n    // neither detail nor docs still exist in the list, they're just\n    // less useful for display purposes.\n    let documented: Vec&lt;&amp;Method&gt; = methods\n        .iter()\n        .filter(|m| m.detail.is_some() || m.documentation.is_some())\n        .collect();\n\n    println!(\n        \"{} of {} Cell&lt;u8&gt; methods have signatures or docs:\\n\",\n        documented.len(),\n        methods.len()\n    );\n\n    for method in documented {\n        // The signature line (e.g. \"pub fn push_str(&amp;mut self, string: &amp;str)\")\n        if let Some(sig) = &amp;method.detail {\n            println!(\"fn {}  →  {sig}\", method.name);\n        } else {\n            println!(\"fn {} (no signature)\", method.name);\n        }\n\n        // Rustdoc\n        if let Some(doc) = &amp;method.documentation {\n            // Just the first sentence to keep output manageable.\n            let first_line = doc.lines().next().unwrap_or(\"\").trim();\n            if !first_line.is_empty() {\n                println!(\"  doc: {first_line}\");\n            }\n        }\n\n        println!();\n    }\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>26 of 26 Cell&lt;u8&gt; methods have signatures or docs:\n\nfn as_ptr  →  const fn(&amp;self) -&gt; *mut T\n  doc: Returns a raw pointer to the underlying data in this cell.\n\nfn clamp  →  fn(self, Self, Self) -&gt; Self\n  doc: Restrict a value to a certain interval.\n\nfn clone  →  fn(&amp;self) -&gt; Self\n  doc: Returns a duplicate of the value.\n\nfn clone_from  →  fn(&amp;mut self, &amp;Self)\n  doc: Performs copy-assignment from `source`.\n\nfn clone_into  →  fn(&amp;self, &amp;mut &lt;Self as ToOwned&gt;::Owned)\n  doc: Uses borrowed data to replace owned data, usually by cloning.\n\nfn cmp  →  fn(&amp;self, &amp;Self) -&gt; Ordering\n  doc: This method returns an [`Ordering`] between `self` and `other`.\n\nfn eq  →  fn(&amp;self, &amp;Rhs) -&gt; bool\n  doc: Equality operator `==`.\n\nfn ge  →  fn(&amp;self, &amp;Rhs) -&gt; bool\n  doc: Tests greater than or equal to (for `self` and `other`) and is used by\n\nfn get  →  const fn(&amp;self) -&gt; T\n  doc: Returns a copy of the contained value.\n\nfn get_mut  →  const fn(&amp;mut self) -&gt; &amp;mut T\n  doc: Returns a mutable reference to the underlying data.\n\nfn gt  →  fn(&amp;self, &amp;Rhs) -&gt; bool\n  doc: Tests greater than (for `self` and `other`) and is used by the `&gt;`\n\nfn into  →  fn(self) -&gt; T\n  doc: Converts this type into the (usually inferred) input type.\n\nfn into_inner  →  const fn(self) -&gt; T\n  doc: Unwraps the value, consuming the cell.\n\nfn le  →  fn(&amp;self, &amp;Rhs) -&gt; bool\n  doc: Tests less than or equal to (for `self` and `other`) and is used by the\n\nfn lt  →  fn(&amp;self, &amp;Rhs) -&gt; bool\n  doc: Tests less than (for `self` and `other`) and is used by the `&lt;` operator.\n\nfn max  →  fn(self, Self) -&gt; Self\n  doc: Compares and returns the maximum of two values.\n\nfn min  →  fn(self, Self) -&gt; Self\n  doc: Compares and returns the minimum of two values.\n\nfn ne  →  fn(&amp;self, &amp;Rhs) -&gt; bool\n  doc: Inequality operator `!=`.\n\nfn partial_cmp  →  fn(&amp;self, &amp;Rhs) -&gt; Option&lt;Ordering&gt;\n  doc: This method returns an ordering between `self` and `other` values if one exists.\n\nfn replace  →  const fn(&amp;self, T) -&gt; T\n  doc: Replaces the contained value with `val`, and returns the old contained value.\n\nfn set  →  const fn(&amp;self, T)\n  doc: Sets the contained value.\n\nfn swap  →  fn(&amp;self, &amp;Cell&lt;T&gt;)\n  doc: Swaps the values of two `Cell`s.\n\nfn take  →  const fn(&amp;self) -&gt; T\n  doc: Takes the value of the cell, leaving `Default::default()` in its place.\n\nfn to_owned  →  fn(&amp;self) -&gt; &lt;Self as ToOwned&gt;::Owned\n  doc: Creates owned data from borrowed data, usually by cloning.\n\nfn try_into  →  fn(self) -&gt; Result&lt;T, &lt;Self as TryInto&lt;T&gt;&gt;::Error&gt;\n  doc: Performs the conversion.\n\nfn update  →  const fn(&amp;self, impl FnOnce(T) -&gt; T)\n  doc: Updates the contained value using a function.\n</code></pre>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/batch-query.html",
      "url": "https://example.com/batch-query.html",
      "title": "Batch Query",
      "content_html": "<h1>Batch Query</h1>\n<p>Querying multiple types and aggregating the results.</p>\n<p><code>query_methods_batch</code> runs each query in parallel: one thread per type,\neach with its own independent <code>rust-analyzer</code> subprocess. Total wall time\nis roughly the cost of the slowest single query rather than the sum of all.\nErrors are per-type and do not abort the batch.</p>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/batch_query.rs\">batch_query.rs</a></h3>\n<p>For <code>Vec&lt;u8&gt;</code>, <code>VecDeque&lt;u8&gt;</code>, and <code>LinkedList&lt;u8&gt;</code>, this example returns:</p>\n<ul>\n<li>Per-type method counts: printed after all queries complete</li>\n<li>Common methods: the intersection across all three types</li>\n<li>Unique methods: what each type has that neither of the others does</li>\n</ul>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_methods_batch};\nuse rust_meth_lib::error::RustMethError;\nuse std::collections::{BTreeMap, BTreeSet};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    let queries: &amp;[(&amp;str, Option&lt;&amp;str&gt;)] = &amp;[\n        (\"Vec&lt;u8&gt;\", None),\n        (\"VecDeque&lt;u8&gt;\", None),\n        (\"LinkedList&lt;u8&gt;\", None),\n    ];\n\n    // All three queries run in parallel. Results come back in input order.\n    let batch_results = query_methods_batch(queries, &amp;ra_path);\n\n    // -- Partition into successes and failures --\n\n    let mut results: BTreeMap&lt;&amp;str, BTreeSet&lt;String&gt;&gt; = BTreeMap::new();\n    let mut failures: Vec&lt;(&amp;str, RustMethError)&gt; = Vec::new();\n\n    for (type_name, result) in batch_results {\n        match result {\n            Ok(methods) =&gt; {\n                let names: BTreeSet&lt;String&gt; = methods.into_iter().map(|m| m.name).collect();\n                println!(\"{type_name}: {} methods\", names.len());\n                results.insert(type_name, names);\n            }\n            Err(e) =&gt; {\n                eprintln!(\"{type_name}: FAILED: {e}\");\n                failures.push((type_name, e));\n            }\n        }\n    }\n\n    println!();\n\n    // -- Methods common to all successfully queried types --\n\n    if results.len() &gt;= 2 {\n        let mut common = results.values().next().cloned().unwrap_or_default();\n        for set in results.values().skip(1) {\n            common = common.intersection(set).cloned().collect();\n        }\n\n        println!(\n            \"Methods common to all {} types ({} total):\",\n            results.len(),\n            common.len()\n        );\n        for name in &amp;common {\n            println!(\"  {name}\");\n        }\n        println!();\n    }\n\n    // -- Per-type unique methods --\n    //\n    // What does each type have that none of the others do?\n\n    for (type_name, methods) in &amp;results {\n        let others_union: BTreeSet&lt;&amp;String&gt; = results\n            .iter()\n            .filter(|(t, _)| *t != type_name)\n            .flat_map(|(_, s)| s.iter())\n            .collect();\n\n        let unique: Vec&lt;&amp;String&gt; = methods\n            .iter()\n            .filter(|m| !others_union.contains(m))\n            .collect();\n\n        if unique.is_empty() {\n            println!(\"{type_name}: no unique methods\");\n        } else {\n            println!(\"{type_name} unique methods ({}):\", unique.len());\n            for name in unique {\n                println!(\"  {name}\");\n            }\n        }\n        println!();\n    }\n\n    // -- Summary of failures --\n\n    if !failures.is_empty() {\n        println!(\"Failed queries:\");\n        for (type_name, err) in failures {\n            println!(\"  {type_name}: {err}\");\n        }\n    }\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>Vec&lt;u8&gt;: 229 methods\nVecDeque&lt;u8&gt;: 114 methods\nLinkedList&lt;u8&gt;: 46 methods\n\nMethods common to all 3 types (32 total):\n  append\n  clamp\n  clear\n  clone\n  clone_from\n  clone_into\n  cmp\n  contains\n  eq\n  extend\n  extend_one\n  extend_reserve\n  extract_if\n  ge\n  gt\n  into\n  into_iter\n  is_empty\n  iter\n  iter_mut\n  le\n  len\n  lt\n  max\n  min\n  ne\n  partial_cmp\n  remove\n  retain\n  split_off\n  to_owned\n  try_into\n\nLinkedList&lt;u8&gt; unique methods (4):\n  cursor_back\n  cursor_back_mut\n  cursor_front\n  cursor_front_mut\n\nVec&lt;u8&gt; unique methods (156):\n  align_to\n  align_to_mut\n  array_windows\n  as_array\n  as_ascii\n  as_ascii_unchecked\n  as_chunks\n  as_chunks_mut\n  as_chunks_unchecked\n  as_chunks_unchecked_mut\n  as_mut\n  as_mut_array\n  as_mut_ptr\n  as_mut_ptr_range\n  as_mut_slice\n  as_non_null\n  as_ptr\n  as_ptr_range\n  as_rchunks\n  as_rchunks_mut\n  as_ref\n  as_simd\n  as_simd_mut\n  as_slice\n  chunk_by\n  chunk_by_mut\n  chunks\n  chunks_exact\n  chunks_exact_mut\n  chunks_mut\n  clone_from_slice\n  concat\n  connect\n  const_make_global\n  copy_from_slice\n  copy_within\n  dedup\n  dedup_by\n  dedup_by_key\n  deref\n  deref_mut\n  element_offset\n  ends_with\n  eq_ignore_ascii_case\n  escape_ascii\n  extend_from_slice\n  fill\n  fill_with\n  first\n  first_chunk\n  first_chunk_mut\n  first_mut\n  get_disjoint_mut\n  get_disjoint_unchecked_mut\n  get_unchecked\n  get_unchecked_mut\n  into_array\n  into_boxed_slice\n  into_chunks\n  into_parts\n  into_parts_with_alloc\n  into_raw_parts\n  into_raw_parts_with_alloc\n  is_ascii\n  is_sorted\n  is_sorted_by\n  is_sorted_by_key\n  join\n  last\n  last_chunk\n  last_chunk_mut\n  last_mut\n  leak\n  make_ascii_lowercase\n  make_ascii_uppercase\n  partial_sort_unstable\n  partial_sort_unstable_by\n  partial_sort_unstable_by_key\n  partition_dedup\n  partition_dedup_by\n  partition_dedup_by_key\n  peek_mut\n  pop\n  pop_if\n  push\n  push_mut\n  push_within_capacity\n  rchunks\n  rchunks_exact\n  rchunks_exact_mut\n  rchunks_mut\n  recycle\n  repeat\n  reverse\n  rsplit\n  rsplit_mut\n  rsplit_once\n  rsplitn\n  rsplitn_mut\n  select_nth_unstable\n  select_nth_unstable_by\n  select_nth_unstable_by_key\n  set_len\n  shift_left\n  shift_right\n  sort\n  sort_by\n  sort_by_cached_key\n  sort_by_key\n  sort_unstable\n  sort_unstable_by\n  sort_unstable_by_key\n  spare_capacity_mut\n  split_at\n  split_at_checked\n  split_at_mut\n  split_at_mut_checked\n  split_at_mut_unchecked\n  split_at_spare_mut\n  split_at_unchecked\n  split_first\n  split_first_chunk\n  split_first_chunk_mut\n  split_first_mut\n  split_inclusive\n  split_inclusive_mut\n  split_last\n  split_last_chunk\n  split_last_chunk_mut\n  split_last_mut\n  split_mut\n  split_once\n  splitn\n  splitn_mut\n  starts_with\n  strip_circumfix\n  strip_prefix\n  strip_suffix\n  subslice_range\n  swap_remove\n  swap_unchecked\n  swap_with_slice\n  to_ascii_lowercase\n  to_ascii_uppercase\n  to_vec\n  to_vec_in\n  trim_ascii\n  trim_ascii_end\n  trim_ascii_start\n  trim_prefix\n  trim_suffix\n  try_remove\n  try_shrink_to\n  try_shrink_to_fit\n  utf8_chunks\n  windows\n\nVecDeque&lt;u8&gt; unique methods (31):\n  as_mut_slices\n  as_slices\n  consume\n  extend_front\n  fill_buf\n  has_data_left\n  is_read_vectored\n  make_contiguous\n  pop_back_if\n  pop_front_if\n  prepend\n  prepend_from_within\n  range\n  range_mut\n  read\n  read_array\n  read_be\n  read_buf\n  read_buf_exact\n  read_exact\n  read_le\n  read_line\n  read_to_end\n  read_to_string\n  read_until\n  read_vectored\n  retain_back\n  skip_until\n  swap_remove_back\n  swap_remove_front\n  truncate_to_range\n</code></pre>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/error-handling.html",
      "url": "https://example.com/error-handling.html",
      "title": "Error Handling",
      "content_html": "<h1>Error Handling</h1>\n<p>All fallible functions in <code>rust-meth-lib</code> return <code>rust_meth_lib::Result&lt;T&gt;</code>, a\ntype alias over a single [<code>RustMethError</code>] enum. This makes it easy to propagate\nerrors with <code>?</code> in your own code, and to convert them into your application’s\nown error type when you need custom messages or behaviour.</p>\n<h2><code>RustMethError</code> variants</h2>\n<table><thead><tr><th>Variant</th><th>Cause</th></tr></thead><tbody>\n<tr><td><code>RustAnalyzerNotFound</code></td><td><code>rust-analyzer</code> not on <code>PATH</code> or in rustup component directory</td></tr>\n<tr><td><code>TypeNotFound</code></td><td>The probed type doesn’t exist or isn’t in scope</td></tr>\n<tr><td><code>FeatureGated</code></td><td>Type exists but requires a feature flag not enabled in the probe</td></tr>\n<tr><td><code>Timeout</code></td><td><code>rust-analyzer</code> failed to respond within the retry budget</td></tr>\n<tr><td><code>Io</code></td><td>OS or filesystem error</td></tr>\n<tr><td><code>Json</code></td><td>JSON serialization or deserialization failure</td></tr>\n<tr><td><code>UnexpectedResponseShape</code></td><td>Structurally valid JSON but missing expected fields</td></tr>\n<tr><td><code>RecvExhausted</code></td><td>Message loop hit its limit without matching a response</td></tr>\n</tbody></table>\n<h2>Custom error types</h2>\n<p>The most common embedding pattern is wrapping <code>RustMethError</code> in your own\n<code>thiserror</code> enum. Since <code>RustMethError</code> implements <code>std::error::Error</code>, it\nconverts with <code>?</code> directly, but a manual <code>From</code> impl lets you intercept\nspecific variants for custom messages before falling through to a generic\nwrapper for everything else.</p>\n<p>The example below intercepts <code>RustAnalyzerNotFound</code> and <code>TypeNotFound</code> to give\nactionable error messages, while passing everything else through as a generic\n<code>QueryFailed</code>.</p>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/custom_error.rs\">custom_error.rs</a></h3>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_methods};\nuse rust_meth_lib::error::RustMethError;\n\n// -- Your application's error type --\n\n#[derive(Debug, thiserror::Error)]\nenum AppError {\n    /// rust-analyzer isn't installed, give a helpful message.\n    #[error(\"rust-analyzer not found. Install it with: rustup component add rust-analyzer\")]\n    RustAnalyzerMissing,\n\n    /// The type the user asked about doesn't exist.\n    #[error(\n        \"Unknown type `{type_name}`. Check the spelling and that any required crate is listed in deps.\"\n    )]\n    UnknownType { type_name: String },\n\n    /// Anything else from the library becomes a generic query failure.\n    #[error(\"LSP query failed: {0}\")]\n    QueryFailed(RustMethError),\n}\n\n// -- Conversion --\n//\n// We can't use `#[from]` for everything because we want to intercept specific\n// variants. Instead, write a manual `From` impl that matches the cases we care\n// about and falls through to the generic wrapper for everything else.\n\nimpl From&lt;RustMethError&gt; for AppError {\n    fn from(err: RustMethError) -&gt; Self {\n        match err {\n            RustMethError::RustAnalyzerNotFound =&gt; Self::RustAnalyzerMissing,\n            RustMethError::TypeNotFound { type_name, .. } =&gt; Self::UnknownType { type_name },\n            other =&gt; Self::QueryFailed(other),\n        }\n    }\n}\n\n// -- Application logic --\n\nfn lookup(type_name: &amp;str) -&gt; Result&lt;(), AppError&gt; {\n    // find_rust_analyzer() returns RustMethError::RustAnalyzerNotFound on\n    // failure, which our From impl converts to AppError::RustAnalyzerMissing.\n    let ra_path = find_rust_analyzer()?;\n\n    // query_methods() returns RustMethError::TypeNotFound for bad type names,\n    // which our From impl converts to AppError::UnknownType.\n    let methods = query_methods(type_name, &amp;ra_path, None)?;\n\n    println!(\"{type_name}: {} methods\", methods.len());\n    for m in methods.iter().take(5) {\n        println!(\"  {}\", m.name);\n    }\n    if methods.len() &gt; 5 {\n        println!(\"  ...and {} more\", methods.len() - 5);\n    }\n    Ok(())\n}\n\nfn main() {\n    // Use a type that will succeed.\n    match lookup(\"Vec&lt;u8&gt;\") {\n        Ok(()) =&gt; {}\n        Err(e) =&gt; eprintln!(\"error: {e}\"),\n    }\n\n    println!();\n\n    // Use a type that will fail with `TypeNotFound`, demonstrates the\n    // custom error message path.\n    match lookup(\"NonExistentType\") {\n        Ok(()) =&gt; {}\n        Err(AppError::UnknownType { type_name }) =&gt; {\n            eprintln!(\"Type not found: `{type_name}`\");\n            eprintln!(\"Tip: check spelling, or pass deps for third-party crates.\");\n        }\n        Err(e) =&gt; eprintln!(\"error: {e}\"),\n    }\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>Vec&lt;u8&gt;: 229 methods\n  align_to\n  align_to_mut\n  allocator\n  append\n  array_windows\n  ...and 224 more\n\nType not found: `NonExistentType`\nTip: check spelling, or pass deps for third-party crates.\n</code></pre>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/filtering.html",
      "url": "https://example.com/filtering.html",
      "title": "Filtering",
      "content_html": "<h1>Filtering</h1>\n<p>Filters and ranks methods against a query string without re-querying\n<code>rust-analyzer</code>. Useful if you already have a <code>Vec&lt;Method&gt;</code> and want to narrow\nresults. Exact matches score highest, then prefix, then substring. Non-matching\nmethods are excluded entirely.</p>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/filter_methods.rs\">filter_methods.rs</a></h3>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_methods};\nuse rust_meth_lib::query::filter_methods;\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n    let methods = query_methods(\"HashMap&lt;String, u32&gt;\", &amp;ra_path, None)?;\n    let filtered = filter_methods(&amp;methods, \"get\");\n    println!(\"HashMap&lt;String, u32&gt; methods matching \\\"get\\\":\\n\");\n    for m in filtered {\n        println!(\"{}\", m.name);\n    }\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>HashMap&lt;String, u32&gt; methods matching \"get\":\n\nget\nget_disjoint_mut\nget_disjoint_unchecked_mut\nget_key_value\nget_mut\n</code></pre>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/fuzzy_filter.rs\">fuzzy_filter.rs</a></h3>\n<p><code>query_methods</code> returns a plain <code>Vec&lt;Method&gt;</code>: already sorted and deduplicated,\nready to filter however you like. This example shows a few patterns: substring\nmatch, signature grep, and a simple ranked scorer. If you’re building a TUI or\neditor plugin on top of the library, this is the pattern to follow.</p>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{Method, find_rust_analyzer, query_methods};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n    let methods = query_methods(\"HashMap&lt;String, u32&gt;\", &amp;ra_path, None)?;\n\n    let query = \"get\";\n\n    println!(\"All methods containing \\\"{query}\\\":\");\n    println!(\"{}\", \"-\".repeat(40));\n\n    // -- 1. Simple substring filter --\n    let matched: Vec&lt;&amp;Method&gt; = methods.iter().filter(|m| m.name.contains(query)).collect();\n\n    for m in &amp;matched {\n        println!(\"  {}\", m.name);\n    }\n\n    println!();\n\n    // -- 2. Signature grep --\n    //\n    // Filter on the `detail` field to find methods with a specific return type.\n    // Useful when you want to narrow results by type, not just name.\n\n    let returns_option: Vec&lt;&amp;Method&gt; = methods\n        .iter()\n        .filter(|m| {\n            m.detail\n                .as_deref()\n                .is_some_and(|sig| sig.contains(\"-&gt; Option\"))\n        })\n        .collect();\n\n    println!(\"Methods returning Option&lt;_&gt;:\");\n    println!(\"{}\", \"-\".repeat(40));\n    for m in &amp;returns_option {\n        println!(\n            \"  {}  →  {}\",\n            m.name,\n            m.detail.as_deref().unwrap_or(\"(no signature)\")\n        );\n    }\n\n    println!();\n\n    // -- 3. Simple ranked scorer --\n    //\n    // Score each method by how closely it matches the query: prefix match\n    // scores higher than substring match, exact match highest. This gives\n    // you a starting point for building fuzzy ranking without pulling in\n    // a whole fuzzy-matching crate.\n\n    println!(\"Ranked results for \\\"{query}\\\":\");\n    println!(\"{}\", \"-\".repeat(40));\n\n    let mut scored: Vec&lt;(u8, &amp;Method)&gt; = methods\n        .iter()\n        .filter_map(|m| {\n            let score = if m.name == query {\n                3 // exact\n            } else if m.name.starts_with(query) {\n                2 // prefix\n            } else if m.name.contains(query) {\n                1 // substring\n            } else {\n                return None;\n            };\n            Some((score, m))\n        })\n        .collect();\n\n    // Sort descending by score; stable within each tier (already alpha-sorted\n    // from query_methods).\n    scored.sort_by_key(|item| std::cmp::Reverse(item.0));\n\n    for (score, m) in scored {\n        let label = match score {\n            3 =&gt; \"exact \",\n            2 =&gt; \"prefix\",\n            _ =&gt; \"substr\",\n        };\n        println!(\"  [{label}]  {}\", m.name);\n    }\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>All methods containing \"get\":\n----------------------------------------\n  get\n  get_disjoint_mut\n  get_disjoint_unchecked_mut\n  get_key_value\n  get_mut\n\nMethods returning Option&lt;_&gt;:\n----------------------------------------\n  get  →  fn(&amp;self, &amp;Q) -&gt; Option&lt;&amp;V&gt;\n  get_key_value  →  fn(&amp;self, &amp;Q) -&gt; Option&lt;(&amp;K, &amp;V)&gt;\n  get_mut  →  fn(&amp;mut self, &amp;Q) -&gt; Option&lt;&amp;mut V&gt;\n  insert  →  fn(&amp;mut self, K, V) -&gt; Option&lt;V&gt;\n  remove  →  fn(&amp;mut self, &amp;Q) -&gt; Option&lt;V&gt;\n  remove_entry  →  fn(&amp;mut self, &amp;Q) -&gt; Option&lt;(K, V)&gt;\n\nRanked results for \"get\":\n----------------------------------------\n  [exact ]  get\n  [prefix]  get_disjoint_mut\n  [prefix]  get_disjoint_unchecked_mut\n  [prefix]  get_key_value\n  [prefix]  get_mut\n</code></pre>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/definitions.html",
      "url": "https://example.com/definitions.html",
      "title": "Definitions",
      "content_html": "<h1>Definitions</h1>\n<p>This chapter covers how to locate the actual source code definitions for methods\nacross standard library and third-party types using <code>rust-analyzer</code>.</p>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/go_to_definition.rs\">go_to_definition.rs</a></h3>\n<ol>\n<li>Direct Definition Queries</li>\n</ol>\n<p>The <code>go_to_definition.rs</code> example demonstrates how to query a specific type and\nmethod combination to find its absolute and relative paths on disk, alongside\nthe line number.</p>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_definition};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    // -- Stdlib method --\n\n    print_definition(\"Vec&lt;u8&gt;\", \"push\", &amp;ra_path, None)?;\n    print_definition(\"String\", \"push_str\", &amp;ra_path, None)?;\n    print_definition(\"HashMap&lt;String, u32&gt;\", \"insert\", &amp;ra_path, None)?;\n\n    // -- Third-party method --\n\n    let serde_deps = r#\"serde_json = \"1.0\"\"#;\n    print_definition(\"serde_json::Value\", \"as_str\", &amp;ra_path, Some(serde_deps))?;\n\n    Ok(())\n}\n\nfn print_definition(\n    type_name: &amp;str,\n    method_name: &amp;str,\n    ra_path: &amp;std::path::Path,\n    deps: Option&lt;&amp;str&gt;,\n) -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    print!(\"{type_name}::{method_name}  →  \");\n\n    match query_definition(type_name, method_name, ra_path, deps)? {\n        Some(def) =&gt; {\n            // `path` is the display-friendly shortened form:\n            //   \"library/core/src/num/uint_macros.rs\"\n            //\n            // `full_path` is the absolute path on disk, useful if you want\n            // to open the file programmatically.\n            //\n            // `line` is 0-indexed (LSP convention). Add 1 for display.\n            println!(\"{}:{}\", def.path, def.line + 1);\n            println!(\"       full: {}\", def.full_path);\n        }\n        None =&gt; {\n            // Not an error, `rust-analyzer` just doesn't have a source\n            // location for this method. Common for compiler intrinsics.\n            println!(\"(no source location found)\");\n        }\n    }\n\n    println!();\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>Vec&lt;u8&gt;::push  →  library/alloc/src/vec/mod.rs:995\n       full: /home/jr/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/alloc/src/vec/mod.rs\n\nString::push_str  →  library/alloc/src/string.rs:1104\n       full: /home/jr/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/alloc/src/string.rs\n\nHashMap&lt;String, u32&gt;::insert  →  library/std/src/collections/hash/map.rs:1332\n       full: /home/jr/.rustup/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/library/std/src/collections/hash/map.rs\n\nserde_json::Value::as_str  →  src/index.crates.io-1949cf8c6b5b557f/serde_json-1.0.151/src/value/mod.rs:492\n       full: /home/jr/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/serde_json-1.0.151/src/value/mod.rs\n</code></pre>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/builder_definitions.rs\">builder_definitions.rs</a></h3>\n<ol start=\"2\">\n<li>Builder Pattern with Definitions</li>\n</ol>\n<p>The <code>builder_definitions.rs</code> example integrates definition lookup into the\n<code>MethodQuery</code> builder. This allows you to filter methods and immediately\nretrieve their source locations in a single workflow.</p>\n<blockquote class=\"markdown-alert-note\">\n<p>For third-party types, each matching method runs a separate definition query.\nUse a tight filter (exact or short prefix) to limit the number of queries.\nResults are not cached by the results cache since definition queries return\nsource locations rather than method lists, expect similar times on every run\nfor third-party types.</p>\n</blockquote>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::find_rust_analyzer;\nuse rust_meth_lib::query::MethodQuery;\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    // -- Stdlib type -- fast --\n\n    let results = MethodQuery::new(\"Vec&lt;u8&gt;\")\n        .filter(\"drain\")\n        .run_with_definitions(&amp;ra_path)?;\n\n    println!(\"Vec&lt;u8&gt; methods matching \\\"drain\\\" with source locations:\");\n    for r in &amp;results {\n        match &amp;r.definition {\n            Some(def) =&gt; println!(\"  {}  →  {}:{}\", r.method.name, def.path, def.line + 1),\n            None =&gt; println!(\"  {}  →  (no source location)\", r.method.name),\n        }\n    }\n\n    println!();\n\n    // -- Third-party type -- use an exact filter to limit definition queries.\n    // Definition results are not cached, so this will always spin up\n    // rust-analyzer. A broad filter like \"as_\" would multiply this cost\n    // by the number of matches.\n\n    println!(\"Querying serde_json::Value::as_str (may be slow on first run)...\");\n    let results = MethodQuery::new(\"serde_json::Value\")\n        .deps(r#\"serde_json = \"1.0\"\"#)\n        .filter(\"as_str\")\n        .run_with_definitions(&amp;ra_path)?;\n\n    println!(\"serde_json::Value methods matching \\\"as_str\\\" with source locations:\");\n    for r in &amp;results {\n        match &amp;r.definition {\n            Some(def) =&gt; println!(\"  {}  →  {}:{}\", r.method.name, def.path, def.line + 1),\n            None =&gt; println!(\"  {}  →  (no source location)\", r.method.name),\n        }\n    }\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>Vec&lt;u8&gt; methods matching \"drain\" with source locations:\n  drain  →  library/alloc/src/vec/mod.rs:3022\n\nQuerying serde_json::Value::as_str (may be slow on first run)...\nserde_json::Value methods matching \"as_str\" with source locations:\n  as_str  →  src/index.crates.io-1949cf8c6b5b557f/serde_json-1.0.151/src/value/mod.rs:492\n</code></pre>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/builder-api.html",
      "url": "https://example.com/builder-api.html",
      "title": "Builder API",
      "content_html": "<h1>Builder API</h1>\n<h2>Builder options</h2>\n<table><thead><tr><th>Method</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>MethodQuery::new(type_name)</code></td><td>Start a query for the given type</td></tr>\n<tr><td><code>.deps(toml)</code></td><td>TOML dependency string for third-party crates</td></tr>\n<tr><td><code>.filter(query)</code></td><td>Narrow results by name (exact &gt; prefix &gt; substring)</td></tr>\n<tr><td><code>.run(&amp;ra_path)</code></td><td>Execute and return <code>Vec&lt;Method&gt;</code></td></tr>\n<tr><td><code>.run_with_definitions(&amp;ra_path)</code></td><td>Execute and return <code>Vec&lt;MethodResult&gt;</code> with source locations</td></tr>\n</tbody></table>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/builder_run.rs\">builder_run.rs</a></h3>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::find_rust_analyzer;\nuse rust_meth_lib::query::MethodQuery;\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    let methods = MethodQuery::new(\"String\").filter(\"push\").run(&amp;ra_path)?;\n\n    println!(\"String methods matching \\\"push\\\":\");\n    for m in &amp;methods {\n        let sig = m.detail.as_deref().unwrap_or(\"(no signature)\");\n        println!(\"  {}  →  {sig}\", m.name);\n    }\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>String methods matching \"push\":\n  push  →  fn(&amp;mut self, char)\n  push_str  →  fn(&amp;mut self, &amp;str)\n</code></pre>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/results-cache.html",
      "url": "https://example.com/results-cache.html",
      "title": "Results Cache",
      "content_html": "<h1>Results Cache</h1>\n<p>The results_cache example demonstrates the caching mechanism introduced in\nversion 0.4.0.</p>\n<p>When querying methods for a type, the library normally spins up a\n<code>rust-analyzer</code> LSP session, which can take a few seconds. The results cache\nstores these outputs as JSON in <code>$XDG_CACHE_HOME/rust-meth/results/</code>. Subsequent\ncalls, even across separate process runs, retrieve the data instantly with zero\nLSP overhead.</p>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/results_cache.rs\">results_cache.rs</a></h3>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_methods};\nuse rust_meth_lib::results_cache::results_cache_entries;\nuse std::time::Instant;\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    // Uncomment to clear the cache on every run\n    // rust_meth_lib::results_cache::clear_results_cache().ok();\n\n    // -- First call -- cold results cache --\n    println!(\"First call (cold cache)...\");\n    let t1 = Instant::now();\n    let methods = query_methods(\"Vec&lt;u8&gt;\", &amp;ra_path, None)?;\n    let elapsed1 = t1.elapsed();\n\n    println!(\"  {} methods found in {:.2?}\\n\", methods.len(), elapsed1);\n\n    // Inspect what was saved\n    let entries = results_cache_entries();\n    println!(\"Results cache now has {} entry:\", entries.len());\n    for e in &amp;entries {\n        println!(\n            \"  {} : {} methods (ra: {})\",\n            e.type_name, e.method_count, e.ra_version\n        );\n    }\n    println!();\n\n    // -- Second call -- results cache hit --\n    println!(\"Second call (warm cache)...\");\n    let t2 = Instant::now();\n    let methods2 = query_methods(\"Vec&lt;u8&gt;\", &amp;ra_path, None)?;\n    let elapsed2 = t2.elapsed();\n\n    println!(\"  {} methods found in {:.2?}\\n\", methods2.len(), elapsed2);\n\n    // Summary\n    let speedup = elapsed1.as_secs_f64() / elapsed2.as_secs_f64();\n    println!(\"Speedup: {speedup:.0}x faster on cache hit\");\n    println!(\"\\nRun this example again to see the persistent cache kick in\");\n    println!(\n        \"on the very first call (~0ms instead of ~{}s).\",\n        elapsed1.as_secs()\n    );\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>First call (cold cache)...\n  229 methods found in 3.66s\n\nResults cache now has 1 entry:\n  Vec&lt;u8&gt; : 229 methods (ra: rust-analyzer 1.100.0-nightly (34baba5 2026-08-16))\n\nSecond call (warm cache)...\n  229 methods found in 17.29ms\n\nSpeedup: 212x faster on cache hit\n\nRun this example again to see the persistent cache kick in\non the very first call (~0ms instead of ~3s).\n</code></pre>\n<blockquote class=\"markdown-alert-note\">\n<p>Running this example a second time will yield a warm cache on both queries,\ndropping the first call’s time down to fractions of a millisecond.</p>\n</blockquote>\n",
      "author": {
        "name": "saylesss88"
      }
    },
    {
      "id": "https://example.com/third-party-crates.html",
      "url": "https://example.com/third-party-crates.html",
      "title": "Third Party Crates",
      "content_html": "<h1>Third Party Crates</h1>\n<p>Querying methods on types from external crates.</p>\n<blockquote class=\"markdown-alert-note\">\n<p>The first time you query a third-party type, Cargo must download and compile\nthe crate inside the ephemeral probe project. This can take 10-30\nseconds on a cold cache. Subsequent queries still require Cargo to resolve\ndependencies inside the ephemeral probe project, so expect similar times on\nevery invocation until persistent probe caching is implemented.</p>\n</blockquote>\n<h3><a href=\"https://github.com/saylesss88/rust-meth/blob/main/lib/examples/third_party_crate.rs\">third_party_crate.rs</a></h3>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::{find_rust_analyzer, query_methods};\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    // ── Single external crate ────────────────────────────────────────────────\n\n    let serde_deps = r#\"serde_json = \"1.0\"\"#;\n\n    println!(\"Querying serde_json::Value methods (may be slow on first run)...\");\n    let methods = query_methods(\"serde_json::Value\", &amp;ra_path, Some(serde_deps))?;\n\n    println!(\"serde_json::Value has {} methods:\", methods.len());\n    for m in &amp;methods {\n        println!(\"  {}\", m.name);\n    }\n\n    println!();\n\n    // ── Multiple dependencies ────────────────────────────────────────────────\n    //\n    // If your type requires more than one crate (e.g. a type that uses serde\n    // traits), pass all deps as a single newline-separated string. This maps\n    // directly to what Cargo expects under [dependencies].\n\n    let multi_deps = \"serde = { version = \\\"1.0\\\", features = [\\\"derive\\\"] }\\nserde_json = \\\"1.0\\\"\";\n\n    println!(\"Querying serde_json::Map with serde + serde_json...\");\n    let map_methods = query_methods(\n        \"serde_json::Map&lt;String, serde_json::Value&gt;\",\n        &amp;ra_path,\n        Some(multi_deps),\n    )?;\n\n    println!(\"serde_json::Map has {} methods:\", map_methods.len());\n    for m in &amp;map_methods {\n        println!(\"  {}\", m.name);\n    }\n\n    Ok(())\n}\n</code></pre>\n<h3>Output</h3>\n<pre><code>Querying serde_json::Value methods (may be slow on first run)...\nserde_json::Value has 37 methods:\n  as_array\n  as_array_mut\n  as_bool\n  as_f64\n  as_i64\n  as_null\n  as_number\n  as_object\n  as_object_mut\n  as_str\n  as_u64\n  clone\n  clone_from\n  clone_into\n  eq\n  get\n  get_mut\n  index\n  index_mut\n  into\n  is_array\n  is_boolean\n  is_f64\n  is_i64\n  is_null\n  is_number\n  is_object\n  is_string\n  is_u64\n  ne\n  pointer\n  pointer_mut\n  sort_all_objects\n  take\n  to_owned\n  to_string\n  try_into\n\nQuerying serde_json::Map with serde + serde_json...\nserde_json::Map has 34 methods:\n  append\n  clear\n  clone\n  clone_from\n  clone_into\n  contains_key\n  entry\n  eq\n  extend\n  extend_one\n  extend_reserve\n  get\n  get_key_value\n  get_mut\n  index\n  index_mut\n  insert\n  into\n  into_iter\n  into_values\n  is_empty\n  iter\n  iter_mut\n  keys\n  len\n  ne\n  remove\n  remove_entry\n  retain\n  sort_keys\n  to_owned\n  try_into\n  values\n  values_mut\n</code></pre>\n<h3>Using the Builder API</h3>\n<p>The same queries using <code>MethodQuery</code>, the <code>.deps()</code> call keeps the dependency\nstring attached to the type it belongs to:</p>\n<pre><code class=\"language-rs\">use rust_meth_lib::analyzer::find_rust_analyzer;\nuse rust_meth_lib::query::MethodQuery;\n\nfn main() -&gt; rust_meth_lib::error::Result&lt;()&gt; {\n    let ra_path = find_rust_analyzer()?;\n\n    let methods = MethodQuery::new(\"serde_json::Value\")\n        .deps(r#\"serde_json = \"1.0\"\"#)\n        .run(&amp;ra_path)?;\n\n    println!(\"serde_json::Value has {} methods:\", methods.len());\n    for m in &amp;methods {\n        println!(\"  {}\", m.name);\n    }\n\n    println!();\n\n    let map_methods = MethodQuery::new(\"serde_json::Map&lt;String, serde_json::Value&gt;\")\n        .deps(\"serde = { version = \\\"1.0\\\", features = [\\\"derive\\\"] }\\nserde_json = \\\"1.0\\\"\")\n        .run(&amp;ra_path)?;\n\n    println!(\"serde_json::Map has {} methods:\", map_methods.len());\n    for m in &amp;map_methods {\n        println!(\"  {}\", m.name);\n    }\n\n    Ok(())\n}\n</code></pre>\n<p>Output is identical to the above.</p>\n",
      "author": {
        "name": "saylesss88"
      }
    }
  ]
}